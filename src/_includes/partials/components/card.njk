<article class="card resource" data-type="{{ resource.data.type | slug }}" data-block="{{ resource.data.buildingBlock | slug }}">
    <h2 class="h4"><a class="text-blue-600 no-underline focus:shadow-none" href="{{ resource.url }}">{{ resource.data.title }}</a></h2>
    <p class=""><strong>{{ resource.data.type }}</strong> for&nbsp;<br class="md:hidden" /><strong>{{ resource.data.buildingBlock }}</strong></p>
</article>
    
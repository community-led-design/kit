{% extends "layouts/base.njk" %}
{% block content %}
<body class="resources">
    {% include "partials/uio.njk" %}
    {% include "partials/header.njk" %}
    <main>
        <article>
            <header>
                <h1>{{ title }}</h1>
            </header>
            <div class="content">
                {{ content | safe }}
                <div class="resources">
                {% for resource in collections.resources %}
                <article class="resource">
                    <h2><a href="{{ resource.url }}">{{ resource.data.title }}</a></h2>
                    <p class="type">{{ resource.data.type }}</p>
                    <p class="building-block">{{ resource.data.buildingBlock }}</p>
                </article>
                {% endfor %}
                </div>
            </div>
        </article>
    </main>
</body>
{% endblock %}

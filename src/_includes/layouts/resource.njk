{% extends "layouts/base.njk" %}

{% block pageHeader %}
<header class="header space-y-4">
    <h1>{{ title | safe}}</h1>
    <p class="subtitle text-2xl"><strong>{{ type }}</strong> for <a href="/building-blocks/{{ buildingBlock | slug }}/"><strong>{{ buildingBlock | title }}</strong></a></p>
</header>
{% endblock %}

{% block content %}
<div class="content">
{{ content | safe }}
</div>
{% endblock %}
